language: java
addons:
  sonarcloud:
    organization: bakkari1u # the key of the org you chose at step #3
    token:
      secure: "*********" # encrypted value of your token
script:
  # the following command line builds the project, runs the tests with coverage and then execute the SonarCloud analysis
  - mvn clean verify sonar:sonar -Pcoverage -Dsonar.projectKey=bakkari1u
